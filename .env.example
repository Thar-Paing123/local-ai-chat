# Optional cloud providers. Never commit a real .env file or API keys.
# Cloud models are enabled only when their key is set.

OPENAI_API_KEY=
XAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Optional provider defaults:
# XAI_MODEL=grok-3-mini
# GEMINI_MODEL=gemini-2.5-flash

# Optional default cloud model:
# CLOUD_MODEL=openai:gpt-4o-mini
